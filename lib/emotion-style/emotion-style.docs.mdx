---
displayName: Emotion Style
description: Optimize Emotion CSS
labels: ["noak", "lib", "emotion-style"]
---

import { EmotionStyle } from "./emotion-style";

### Usage

```tsx
import { EmotionStyle } from "@hoomies/noak.lib.emotion-style";
const initialProps = await Document.getInitialProps(ctx);
const styles = EmotionStyle(initialProps);
```

### Optimize Emotion inside Next Document

```tsx
import Document, {
  DocumentContext,
  Html,
  Head,
  Main,
  NextScript,
} from "next/document";
import { EmotionStyle } from "@hoomies/noak.lib.emotion-style";

export default class MyDocument extends Document {
  static async getInitialProps(ctx: DocumentContext) {
    const initialProps = await Document.getInitialProps(ctx);
    const styles = EmotionStyle(initialProps);

    return {
      ...initialProps,
      styles: styles,
    };
  }

  render() {
    return (
      <Html>
        <Head />
        <body>
          <Main />
          <NextScript />
        </body>
      </Html>
    );
  }
}
```
